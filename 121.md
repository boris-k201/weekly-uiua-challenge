# 2026-08-13

## TASK #1 › Invert Bit

Submitted by: Mohammad S Anwar

You are given integers 0 <= $m <= 255 and 1 <= $n <= 8.
Write a script to invert $n bit from the end of the binary representation of $m and print the decimal representation of the new binary number.

### Uiua

```
InvertBit ← (
  ⊙(↥0-1)
  ⬚0↯[8]⊥2
  ⍜(⊢˜↻)¬
  ⌝⊥2
)
InvertBit 12 3
InvertBit 18 4
```

## TASK #2 › The Travelling Salesman

Submitted by: Jorg Sommrey

You are given a NxN matrix containing the distances between N cities.
Write a script to find a round trip of minimum length visiting all N cities exactly once and returning to the start.

### Uiua

```
# Experimental!
┌─╴Util
  PathLength ← (
    ⊙↯⟜⧻⧈⊂
    /+≡(⊢⊢↻)
  )
  Swap ← (
    °⊟⊏⊸⍖⊂
    ⊂₅⊃(⋅↙|⊏⊙◌|↘1↙⊙↘⊸˜-|⊏◌|↘+1⊙◌)
  )
  GenerateDistMatrix ← (
    ×˜↯⊙˙⊂+1⁅×9˜gen0⊸°√⟜(≠0˙⊞-⇡)
  )
  GenerateDistMatrixSym ← (
    +⊸⍉×˜↯⊙˙⊂+1⁅×9˜gen0⊸°√⟜(>0˙⊞-⇡)
  )
└─╴

┌─╴TravellingSalesman
  Brute ← (
    ⊸⧻°⊏
    ≡(⧈(⊂)˜⊂⊸⊢)⧅≠
    ⊙↯⟜⧻
    ⤚≡(⍢(↘1⟜₂(⍜↻(⊢⊢)⊢)|>0⧻))
    ∩⌞(⊢⊏)⊸⍏≡/+
    ⊙(˜⊂⊸⊢⊢⍉)
  )
  Greedy ← (
    ¤°⊂°⊏
    ⍢(⊂⟜(⍜↻°⊂⊢⍏⤚⊏⊸˜⊙⊏⊣)|>0⋅⧻)
    ˜⊂0⊙◌
    ⤚Util.PathLength
  )
  TwoOpt ← (
    Iter ← (
      ⟜₂(⊢
        ⊸₂({⊙∘}⤚Util.PathLength Util.Swap⊙⊙(°□⊣))
        ◡∩(°□⊢)
        # &p◡$"_>_"
        >
        ◌⨬(∘|⟜₃(⊙⋅⋅◌)))
      ↘1
    )
    Iter₂ ← (
      +1
      ◡(°□⋅⊣)
      ⍜⇌(↘1)↘1
      ⊸˜↘
      ⍢(Iter|>0⧻)
      ⟜₁⊸₂⋅⋅⋅∘◌
    )
    ˜⊂0°⊏
    ⟜⊸Util.PathLength
    ˜⊂∩□
    ⟜˜⊙∘
    0
    ⍥(⍢(Iter₂|>⊙(-3⧻°□⊣))×0) 5
    ∩°□°⊟⋅⊙⋅◌
  )
└─╴

[ [0 5 2 7]
  [5 0 5 3]
  [3 1 0 6]
  [4 5 4 0]]
TravellingSalesman.Brute
&p$"1. len = _, tour = _"
Util.GenerateDistMatrixSym 20
# TravellingSalesman.Greedy
TravellingSalesman.TwoOpt
&p$"2. len = _, tour = _"
```
