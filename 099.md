# 2026-08-08

## TASK #1 › Pattern Match

Submitted by: Mohammad S Anwar

You are given a string $S and a pattern $P.
Write a script to check if given pattern validate the entire string. Print 1 if pass otherwise 0.
The patterns can also have the following characters:
? - Match any single character.
* - Match any sequence of characters.

### Uiua

```
Test ← (
  ↙2
  ⊸(⊗"*?"⊢)
  ⨬(=⊙⊢⊣⍢(⊙(↘1)|×⊃(>0⋅⧻|≠⊙⊢⊣)) # star
  | 1                          # question mark
  | =⊢⊙⊢                       # not special symbol
  )
)

PatternMatch ← (
  ˜⊂"  "

  ⍢(⊸(⊗"*?"⊢)
    ⨬(↘1⍜(⊏1)⍢(⊙(↘1)|≠⊙⊢)|∩(↘1)|∩(↘1))
  | ⨬(0|Test)◡(×∩(>0⧻)))
  ×⊃(⨬(0|/×="  ")⊸(=2⧻)|⋅(=0⧻))
)

PatternMatch "a*e" "abcde"
PatternMatch "a*d" "abcde"
PatternMatch "?b*d" "abcde"
PatternMatch "a*c?e" "abcde"
```

## TASK #2 › Unique Subsequence

Submitted by: Mohammad S Anwar

You are given two strings $S and $T.
Write a script to find out count of different unique subsequences matching $T without changing the position of characters.

### Uiua

```
UniqueSubsequence = /+≡₁(/×=)˜⧅≤⊙⊸⧻

UniqueSubsequence "littleit" "lit"
UniqueSubsequence "london" "lon"
```
