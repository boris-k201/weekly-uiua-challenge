# 2026-08-07

## TASK #1 › Group Anagrams

Submitted by: Mohammad S Anwar

You are given an array of strings @S.
Write a script to group Anagrams together in any random order.
An Anagram is a word or phrase formed by rearranging the letters of a different word or phrase, typically using all the original letters exactly once.

### Uiua

```
GroupAnagrams ← (
  ⊜□+1⊛⊸⍚⍆⊏⍖⊸⍚⍆
)

GroupAnagrams {"opt" "bat" "saw" "tab" "pot" "top" "was"}
GroupAnagrams {"x"}
```

## TASK #2 › Binary Tree to Linked List

Submitted by: Mohammad S Anwar

You are given a binary tree.
Write a script to represent the given binary tree as an object and flatten it to a linked list object. Finally print the linked list object.

### Uiua

```
Iter ← ⍜°□(
  =1⊸⧻
  ⨬(⊸≡(⧻⊥2)⇡₁⊸⧻⟜⊢
    /(⊂∩°□)⊜(□≠0⊂˜▽0⟜↘⌊÷2⊸⧻)
    ∩(⊂0↘1)⊸¬
    ⊃(⊏⊚◌|⊏⊚⊙◌)
    ⇌⊂₃∩₃□
  | ∘)
)
BinaryTreeToLinkedList ← (
  {∘}
  °□⊢/(⊂∩°□)≡(Iter)
  ⍢(/(⊂∩°□)≡(Iter)|>1/↥≡(⧻°□))
  /(⊂∩°□)
  ⊏⊚⊸≠NaN
)

[1 2 3 4 5 NaN NaN NaN NaN 6 7 NaN NaN NaN NaN]
BinaryTreeToLinkedList
```
