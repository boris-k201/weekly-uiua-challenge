# 2026-06-25

## Task 1: Strong and weak primes

Write a script to generate first 10 strong and weak prime numbers.
For example, the nth prime number is represented by p(n).

```
p(1) = 2
p(2) = 3
p(3) = 5
p(4) = 7
p(5) = 11

Strong Prime number p(n) when p(n) > [ p(n-1) + p(n+1) ] / 2
Weak   Prime number p(n) when p(n) < [ p(n-1) + p(n+1) ] / 2
```

### Uiua

```
Prime    ← ↧1/×◿⊸(⇡₂ ⌊√)
IsStrong ← |3 >÷2/+°⊂⌟ ↻¯1 ⊂₃
Primes   ← memo(↘2⊚≡Prime⇡ 1000)

&p "Weak: "
&p ↙ 10 ⊏+1⊚⊸⧈(¬IsStrong) Primes
&p "Strong: "
&p ↙ 10 ⊏+1⊚⊸⧈(IsStrong) Primes
```

## Task 2: Vigenère cipher

Write a script to implement Vigenère cipher. The script should be able encode and decode. Checkout wiki page for more information.

### Uiua

```
Encrypt ← (
  ⊙⟜(×⊙(-@a)+>26⟜(<0)⊸-@a) # get array of non-letters
  ˜⊏⊙(◿)⟜⧻⊙(⇡⊸⧻) # reshape key to match plaintext
  -@a⊙(-@a) # turn letters into numbers
  +@a◿26+ # add key and text mod 26, return into letters
  ≡₂(˜(⨬(∘◌|+@a)⊸≠0)) # insert back the punctuation
)
Antikey ← +@a+26¯-@a
Decrypt ← (
  Antikey
  Encrypt
)
"attacking, tonight"
"oculorhinolaryngology"
Encrypt
"oculorhinolaryngology"
Decrypt
```
